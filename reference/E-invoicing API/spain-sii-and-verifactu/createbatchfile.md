---
title: Export invoices to a new file using the template's format. (sync or async)
excerpt: >
  Export invoices based on the provided filter.

  - If `async` is true, the file will be exported asynchronously and sent to
  your email.

  - If `async` is false, the file will be exported synchronously and returned
  directly max invoices 100,000.
api:
  file: api-docs.json
  operationId: createBatchFile
hidden: false
---