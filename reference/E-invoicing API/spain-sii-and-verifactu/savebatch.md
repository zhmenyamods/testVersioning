---
title: Create invoices per batch file (CSV, EXCEL)
excerpt: >-
  Creates a batch of invoices based on the `sync` flag and `countryIsoCode`. If
  `sync` is true, the file will be processed synchronously. If `sync` is false,
  the file will be processed asynchronously and sent to your email.
api:
  file: api-docs.json
  operationId: saveBatch
hidden: false
---